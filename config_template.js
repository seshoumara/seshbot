const botname = "[seshbot]";
const channels = [ "" ];
const username = "";
const token = "";

const credentials = {
    channels: channels,
    identity: {
        username: username,
        password: token
    }
};

module.exports = {
    botname: botname,
    credentials: credentials
};
